# Lịch sử Console và Logs (File ghi chú quá trình chạy, thay đổi liên tục)
.console_history
logs/
*.log

# Caches, thư viện và phiên bản tải về (Server tự động sinh ra khi chạy)
cache/
libraries/
versions/
usercache.json
version_history.json

# Dữ liệu người chơi & Trạng thái máy chủ (Không nên đồng bộ lên Git)
banned-ips.json
banned-players.json
ops.json
whitelist.json

# Dữ liệu Bản đồ / World 
world_nether/
world_the_end/
world/
lobby/

# Các file tự động sinh khác
wepif.yml

# --- BỔ SUNG: Dữ liệu và rác của Plugins ---
# Bỏ qua các file database cục bộ do plugin sinh ra
*.db
*.sqlite
*.sqlite-journal